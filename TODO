1. locust
2. seed data continously ?
3. queries on superset
4. queries on api ?
    get country dimension also
    we need to chart superset->trino-> postgres dashbaords , so we can show tommorow during standup
    also lets make measurement df and a table